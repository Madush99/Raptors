<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppingapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_size_invert"
        android:label="WTF_Helinsa"
        android:roundIcon="@drawable/logo_size_invert"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addPayment">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".edit_deletePayDetails"></activity>
        <activity android:name=".control_panel">

        </activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>